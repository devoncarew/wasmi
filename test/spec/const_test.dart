wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.252.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.253.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.253.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.254.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.254.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.255.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.255.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.256.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.256.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.257.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.257.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.258.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.258.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.259.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.259.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.260.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.260.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.261.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.261.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.262.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.262.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.263.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.263.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.264.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.264.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800003'));
  });

  group('const.265.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.265.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800003'));
  });

  group('const.266.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.266.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800000'));
  });

  group('const.267.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.267.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800000'));
  });

  group('const.268.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.268.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.269.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.269.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.270.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.270.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.271.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.271.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.272.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.272.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.273.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.273.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.274.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.274.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.275.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.275.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.276.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.276.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.277.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.277.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.278.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.278.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.279.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.279.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.280.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.280.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800000'));
  });

  group('const.281.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.281.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800000'));
  });

  group('const.282.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.282.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.283.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.283.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.284.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.284.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800001'));
  });

  group('const.285.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.285.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800001'));
  });

  group('const.286.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.286.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('58800002'));
  });

  group('const.287.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.287.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('D8800002'));
  });

  group('const.288.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.288.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('0'));
  });

  group('const.289.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.289.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000000'));
  });

  group('const.290.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.290.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('1'));
  });

  group('const.291.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.291.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000001'));
  });

  group('const.292.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.292.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('1'));
  });

  group('const.293.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.293.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000001'));
  });

  group('const.294.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.294.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('1'));
  });

  group('const.295.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.295.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000001'));
  });

  group('const.296.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.296.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('1'));
  });

  group('const.297.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.297.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000001'));
  });

  group('const.298.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.298.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('1'));
  });

  group('const.299.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.299.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000001'));
  });

  group('const.300.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.300.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.301.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.301.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.302.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.302.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.303.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.303.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.304.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.304.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.305.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.305.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.306.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.306.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.307.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.307.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.308.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.308.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.309.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.309.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.310.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.310.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.311.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.311.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.312.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.312.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('2'));
  });

  group('const.313.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.313.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000002'));
  });

  group('const.314.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.314.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('3'));
  });

  group('const.315.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.315.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('80000003'));
  });

  group('const.316.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.316.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('7F7FFFFF'));
  });

  group('const.317.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.317.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('FF7FFFFF'));
  });

  group('const.318.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.318.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('7F7FFFFF'));
  });

  group('const.319.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.319.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('FF7FFFFF'));
  });

  group('const.320.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.320.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('7F7FFFFF'));
  });

  group('const.321.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.321.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f32('FF7FFFFF'));
  });

  group('const.322.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.322.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000000'));
  });

  group('const.323.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.323.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000000'));
  });

  group('const.324.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.324.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.325.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.325.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.326.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.326.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.327.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.327.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.328.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.328.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.329.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.329.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.330.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.330.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.331.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.331.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.332.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.332.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.333.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.333.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.334.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.334.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.335.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.335.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.336.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.336.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.337.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.337.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.338.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.338.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.339.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.339.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.340.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.340.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.341.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.341.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.342.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.342.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.343.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.343.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.344.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.344.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.345.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.345.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.346.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.346.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000003'));
  });

  group('const.347.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.347.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000003'));
  });

  group('const.348.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.348.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000000'));
  });

  group('const.349.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.349.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000000'));
  });

  group('const.350.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.350.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.351.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.351.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.352.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.352.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.353.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.353.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.354.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.354.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.355.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.355.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.356.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.356.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.357.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.357.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.358.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.358.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000001'));
  });

  group('const.359.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.359.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000001'));
  });

  group('const.360.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.360.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.361.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.361.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.362.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.362.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.363.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.363.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.364.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.364.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.365.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.365.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.366.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.366.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.367.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.367.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.368.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.368.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.369.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.369.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.370.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.370.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000002'));
  });

  group('const.371.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.371.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000002'));
  });

  group('const.372.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.372.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1A70000000000003'));
  });

  group('const.373.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.373.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('9A70000000000003'));
  });

  group('const.374.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.374.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7E60000000000000'));
  });

  group('const.375.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.375.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FE60000000000000'));
  });

  group('const.376.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.376.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7E60000000000001'));
  });

  group('const.377.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.377.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FE60000000000001'));
  });

  group('const.378.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.378.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7E60000000000001'));
  });

  group('const.379.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.379.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FE60000000000001'));
  });

  group('const.380.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.380.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7E60000000000002'));
  });

  group('const.381.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.381.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FE60000000000002'));
  });

  group('const.382.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.382.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000000'));
  });

  group('const.383.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.383.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000000'));
  });

  group('const.384.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.384.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000001'));
  });

  group('const.385.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.385.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000001'));
  });

  group('const.386.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.386.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000001'));
  });

  group('const.387.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.387.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000001'));
  });

  group('const.388.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.388.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000001'));
  });

  group('const.389.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.389.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000001'));
  });

  group('const.390.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.390.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000001'));
  });

  group('const.391.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.391.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000001'));
  });

  group('const.392.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.392.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000001'));
  });

  group('const.393.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.393.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000001'));
  });

  group('const.394.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.394.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.395.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.395.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.396.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.396.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.397.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.397.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.398.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.398.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.399.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.399.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.400.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.400.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.401.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.401.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.402.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.402.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.403.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.403.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.404.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.404.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.405.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.405.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.406.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.406.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000002'));
  });

  group('const.407.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.407.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000002'));
  });

  group('const.408.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.408.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('6570000000000003'));
  });

  group('const.409.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.409.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('E570000000000003'));
  });

  group('const.410.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.410.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000000'));
  });

  group('const.411.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.411.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000000'));
  });

  group('const.412.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.412.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000001'));
  });

  group('const.413.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.413.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000001'));
  });

  group('const.414.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.414.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000001'));
  });

  group('const.415.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.415.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000001'));
  });

  group('const.416.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.416.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000001'));
  });

  group('const.417.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.417.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000001'));
  });

  group('const.418.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.418.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000001'));
  });

  group('const.419.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.419.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000001'));
  });

  group('const.420.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.420.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000001'));
  });

  group('const.421.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.421.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000001'));
  });

  group('const.422.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.422.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.423.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.423.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.424.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.424.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.425.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.425.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.426.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.426.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.427.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.427.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.428.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.428.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.429.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.429.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.430.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.430.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.431.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.431.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.432.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.432.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.433.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.433.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.434.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.434.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000002'));
  });

  group('const.435.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.435.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000002'));
  });

  group('const.436.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.436.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('4600000000000003'));
  });

  group('const.437.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.437.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C600000000000003'));
  });

  group('const.438.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.438.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('43B0000000000000'));
  });

  group('const.439.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.439.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C3B0000000000000'));
  });

  group('const.440.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.440.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('43B0000000000001'));
  });

  group('const.441.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.441.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C3B0000000000001'));
  });

  group('const.442.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.442.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('43B0000000000001'));
  });

  group('const.443.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.443.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C3B0000000000001'));
  });

  group('const.444.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.444.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('43B0000000000002'));
  });

  group('const.445.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.445.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('C3B0000000000002'));
  });

  group('const.446.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.446.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('0'));
  });

  group('const.447.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.447.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000000'));
  });

  group('const.448.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.448.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1'));
  });

  group('const.449.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.449.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000001'));
  });

  group('const.450.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.450.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1'));
  });

  group('const.451.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.451.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000001'));
  });

  group('const.452.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.452.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1'));
  });

  group('const.453.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.453.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000001'));
  });

  group('const.454.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.454.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1'));
  });

  group('const.455.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.455.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000001'));
  });

  group('const.456.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.456.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('1'));
  });

  group('const.457.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.457.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000001'));
  });

  group('const.458.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.458.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.459.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.459.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.460.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.460.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.461.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.461.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.462.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.462.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.463.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.463.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.464.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.464.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.465.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.465.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.466.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.466.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.467.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.467.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.468.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.468.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.469.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.469.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.470.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.470.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('2'));
  });

  group('const.471.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.471.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8000000000000002'));
  });

  group('const.472.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.472.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('10000000000003'));
  });

  group('const.473.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.473.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('8010000000000003'));
  });

  group('const.474.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.474.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7FEFFFFFFFFFFFFF'));
  });

  group('const.475.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.475.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FFEFFFFFFFFFFFFF'));
  });

  group('const.476.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.476.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('7FEFFFFFFFFFFFFF'));
  });

  group('const.477.wasm', () {
    late ModuleDefinition def;
    late Module m;

    setUpAll(() {
      def = ModuleDefinition.parse(File('test/spec/const/const.477.wasm'));
      m = Module(def, imports: {'spectest': specTestModule(), ...registered});
    });

    returns('f_0', () => m.$('f', []), $f64('FFEFFFFFFFFFFFFF'));
  });
}
